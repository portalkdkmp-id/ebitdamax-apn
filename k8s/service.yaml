apiVersion: v1
kind: Service
metadata:
  name: ebitda
  namespace: ebitda
  labels:
    app.kubernetes.io/name: ebitda
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ebitda
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: http
